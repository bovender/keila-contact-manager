<% content_for :title, "New project" %>

<div class="w-full max-w-2xl">
  <%= render "shared/flash" %>
  <h1 class="font-bold text-3xl text-gray-900 mb-6">New project</h1>
  <%= render "form", keila_project: @keila_project, url: keila_projects_path %>
</div>
